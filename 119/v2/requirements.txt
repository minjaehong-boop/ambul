# Triage RAG — Python dependencies
# Python >= 3.10

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# LangChain
langchain-core>=0.2.0
langchain-openai>=0.1.0

# Agents SDK (triage_rag_agent only)
openai-agents>=0.0.7

# PDF parsing
pdfplumber>=0.10.0

# YAML
pyyaml>=6.0

# Configuration
dataclass-wizard>=0.22.0

# Input sanitization
bleach>=6.1.0

# OpenTelemetry (optional — only needed if ENABLE_TRACING=true)
# opentelemetry-sdk>=1.20.0
# opentelemetry-exporter-otlp>=1.20.0
